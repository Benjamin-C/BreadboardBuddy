% lettersubsections

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{bendefault}[Default Package for things Ben does]

\usepackage[margin=1in]{geometry}

% Prepare the images
\RequirePackage{graphicx}
\graphicspath{ {} }
\RequirePackage{float}

\newcommand{\sectionnum}{\thesection.}
\newcommand{\subsectionnum}{\thesubsection.}

\DeclareOption{onlysubsectionlabels}{\renewcommand\sectionnum{}}
\DeclareOption{onlysubsubsectionlabels}{\renewcommand\subsectionnum{}}

% Default to using numbers for section labels
\renewcommand\thesection{\arabic{section}}
\renewcommand\thesubsection{\sectionnum \arabic{subsection}}
\renewcommand\thesubsubsection{\subsectionnum \arabic{subsubsection}}

% Label [sub][sub]sections with letters
\DeclareOption{lettersections}{\renewcommand\thesection{\alph{section}}}
\DeclareOption{lettersubsections}{\renewcommand\thesubsection{\sectionnum \alph{subsection}}}
\DeclareOption{lettersubsubsections}{\renewcommand\thesubsubsection{\subsectionnum \alph{subsubsection}}}

% Label [sub][sub]sections with letters
\DeclareOption{Lettersections}{\renewcommand\thesection{\Alph{section}}}
\DeclareOption{Lettersubsections}{\renewcommand\thesubsection{\sectionnum \Alph{subsection}}}
\DeclareOption{Lettersubsubsections}{\renewcommand\thesubsubsection{\subsectionnum \Alph{subsubsection}}}

% Label [sub][sub]sections with letters
\DeclareOption{romansections}{\renewcommand\thesection{\roman{section}}}
\DeclareOption{romansubsections}{\renewcommand\thesubsection{\sectionnum \roman{subsection}}}
\DeclareOption{romansubsubsections}{\renewcommand\thesubsubsection{\subsectionnum \roman{subsubsection}}}

% Label [sub][sub]sections with letters
\DeclareOption{Romansections}{\renewcommand\thesection{\Roman{section}}}
\DeclareOption{Romansubsections}{\renewcommand\thesubsection{\sectionnum \Roman{subsection}}}
\DeclareOption{Romansubsubsections}{\renewcommand\thesubsubsection{\subsectionnum \Roman{subsubsection}}}

\DeclareOption{nosections}{\renewcommand\thesection{}}
\DeclareOption{nosubsections}{\renewcommand\thesubsection{}}
\DeclareOption{nosubsubsections}{\renewcommand\thesubsubsection{}}

% Option to allow hyphens in ComputerModern Typewriter fonts
\newcommand{\hypenatoption}{}
\DeclareOption{hyphenatelistings}{\renewcommand\hypenatoption{htt}}
\ProcessOptions\relax

% Allow the use of links
\RequirePackage[hidelinks]{hyperref}

% Allow enumerate to include letters
\RequirePackage[shortlabels]{enumitem}

% Don't hypnehate words
% \RequirePackage[\hypenatoption]{hyphenat}
% \RequirePackage[none]{hyphenat}

% Color rows of tables
\RequirePackage[table,x11names]{xcolor}

% Code listings
\RequirePackage{listings}
% \RequirePackage{xcolor}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.6,0.6,0.6}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\lstdefinestyle{black}{
    % backgroundcolor=\color{backcolour},
    commentstyle=\color{black},
    keywordstyle=\color{black},
    numberstyle=\small\color{black},
    stringstyle=\color{black},
    basicstyle=\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    postbreak=\mbox{\textcolor{black}{$\hookrightarrow$}\space},
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=4,
    basicstyle=\ttfamily,
    language=Verilog,
    columns=fullflexible
}
\lstdefinestyle{mystyle}{
    % backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen}, % colorgreen
    keywordstyle=\color{red},
    numberstyle=\small\color{codegray}, % codegray
    stringstyle=\color{codepurple}, % codepurple
    basicstyle=\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=4,
    basicstyle=\ttfamily,
    language=Verilog,
    columns=fullflexible
}
\lstset{style=mystyle}

% \addimgfigure label, path, caption, width, angle
\newcommand{\addimgfigure}[5]{
\begin{figure}[H]
  \centering
  \makebox[\textwidth][c]{
  \includegraphics[width=#4\textwidth,angle=#5]{#2}
  }
  \caption{#3}
  \label{#1}
\end{figure}
}

\newcommand{\code}[1]{\texttt{#1}}

% Easy center text
\newcommand{\centerit}[1]{\begin{center}#1\end{center}}

% Prepare the title and move it up slightly
\RequirePackage{titling}
\setlength{\droptitle}{-4em}

\RequirePackage{caption}
% \captionsetup[table]{labelsep=space}

\newcommand{\blankline}{
% Important blank line below, do not remove

\vspace{5pt}}

\newcommand{\twocol}[2]{
\blankline
\begin{minipage}{0.47\textwidth}
  #1
\end{minipage}
\begin{minipage}{0.5\textwidth}
  #2
\end{minipage}
\blankline
}

% Don't hyphenate words
\righthyphenmin=63


% Multirows cells in tables
\RequirePackage{multirow}
% Multiple column text
\RequirePackage{multicol}
% Math stuff
\RequirePackage{amsmath}

\NewDocumentCommand{\rot}{O{60} O{1.2em} m}{\makebox[#2][l]{\rotatebox{#1}{#3}}}%

% The question sections
\newcounter{questnum}
\newcommand{\question}[1]{
  \refstepcounter{questnum}
  {\vspace{1em}\normalfont\bfseries\large\noindent\hspace{2.7em} Q\thequestnum: #1 \vspace{0.2em}\\}
}

% Degree sign
\newcommand{\degree}{^\circ}

% Simple names for text formatting
\newcommand{\italic}[1]{%
\textit{#1}%
}
\newcommand{\bold}[1]{%
\textbf{#1}%
}

% Prettier web links
\newcommand{\weblink}[2]{\ifthenelse{\equal{#1}{*}}%
  {\weblinkStar}%
  {\weblinkNoStar{#1}}%
}
% \newcommand{\weblinkStar}{starred version}
% \newcommand{\weblinkNoStar}[1]{normal version}

\newcommand{\weblinkNoStar}[2]{%
{\color{blue} \underline{\href{#1}{#2}}}%
}% Prettier web link
\newcommand{\weblinkStar}[1]{%
{\color{blue} \underline{\href{#1}{#1}}}%
}

% Horizontal line
\newcommand{\horizline}{
\noindent\makebox[\linewidth]{\rule{\textwidth}{0.1pt}}
}

% Stuff that needs to be done
\newcommand{\todo}[1]{
{\Huge\color{red} TODO: #1}
}

% Column break in multicols enviornment
\newcommand{\newcol}{
	\vfill\null\columnbreak
}

% subcaptions on figures
\usepackage{subcaption}
